<?xml version="1.0" encoding="utf-8"?>
<LinearLayout xmlns:android="http://schemas.android.com/apk/res/android"
    android:layout_width="fill_parent"
    android:layout_height="fill_parent"
    android:orientation="vertical" >

    <ListView
        android:id="@+id/verseListView"
        android:layout_width="match_parent"
        android:layout_height="match_parent"
        android:background="@color/verseListView_background"
        android:divider="@color/verseListView_divider"
        android:dividerHeight="@dimen/verseListView_dividerHeight"
        android:paddingTop="@dimen/verseListView_paddingVertical" >

    </ListView>

</LinearLayout>